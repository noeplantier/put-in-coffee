import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://votre-site.com', // Remplacez par l'URL de votre site
  integrations: [
    // Ajoutez ici vos intégrations si nécessaire
  ],
  build: {
    // Options de construction spécifiques
  },
  markdown: {
    // Options pour le traitement Markdown
  },
});